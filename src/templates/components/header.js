import React, {PropTypes} from 'react';
import Switcher from './switchLang';

const Header = () => {
    return (
        <header>
            <Switcher/>
        </header>
    )
}
export default Header;